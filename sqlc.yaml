version: "2"
sql:
  - engine: "sqlite"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "store/db"
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false
